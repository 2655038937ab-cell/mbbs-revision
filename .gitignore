# ---------- Python ----------
.venv/
.venv-py39/
__pycache__/
*.pyc
*.log

# ---------- Secrets & data: never commit ----------
config.json
config.json.bak-*
data/
data-pku/
*.db
*.db-wal
*.db-shm
*.db.bak-*
google-service-account.json

# ---------- OS / editor ----------
.DS_Store
node_modules/

# ---------- Machine-specific helpers ----------
# LaunchAgent/startup files carry the absolute paths of the machine that produced
# them, so they are neither useful nor safe to publish.
macos-autostart.plist
macos-autostart.plist.patched
.macos-autostart.plist
.macos-autostart.plist.patched
setup-autostart.sh

# ---------- One-off / dev-only ----------
test_*.py
test_*.js
smoke_test.js
check_modules.mjs
check_rendering.mjs
recompress_lessons.py
*deploy.zip
*.bak
*.bak-*

# One-off data repairs written against a specific private library.
# `exporters.py` is NOT one of these — it is imported by server.py and is required.
quiz_audit.py
quiz_apply_repairs.py
quiz_fix_slides.py
repair_quiz_questions.py

# ---------- Private material ----------
reviews/
textbook_parts/
deepseek harness/
测试/
GOOGLE_DRIVE_SETUP.md
# ---------- 只留在本地的服务器笔记（含 IP/域名等环境信息，不进仓库）----------
deploy/NEW-PROJECT.md
